# South Prosecution Study

Static Hebrew study site for internship interview preparation focused on the Civil Division of the Southern District Prosecutor's Office.

## Local Use

Open `index.html` directly or serve the folder with a static server:

```powershell
python -m http.server 8766 --bind 127.0.0.1
```
